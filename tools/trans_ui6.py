# -*- coding: utf-8 -*-
"""UI 6차 — 상단 **탭바 제목**(`ステータス<＞>`).

실기: 스테이터스 화면 상단 탭이 「ステータス▶」로 통째 미번역인데, 옆의
「스킬」탭은 멀쩡히 나왔다.

★★원인 = 이 문자열이 **자기만의 NUL 구분 조각이 아니다**. 바로 앞에
  12개짜리 32비트 포인터 표(`0607FF0C 0608AC04 0609EE80 …` — 새턴 Work RAM
  High `0x06xxxxxx` 주소들)가 붙어 있는데 그 표에 **0x00 바이트가 하나도
  없어서**, NUL 순차 스캔이 「포인터표 + 텍스트」를 65바이트짜리 한 조각으로
  읽어 버렸다. 디코드하면 당연히 `None` 이라 후보에서 탈락 → 한 번도 검출 안 됨.
  (같은 탭바의 `<＜>スキル` 은 앞이 NUL 로 끊겨 있어서 정상 검출됐다.)

그래서 여기서는 스캔이 아니라 **원문 바이트열 정확 일치**로 자리를 찾는다
([[feedback_regex_string_start_backtrace]] 와 같은 이유 — 조각 시작점을
스캐너에게 맡길 수 없다).

`<＞>` `<＜>` 는 탭 좌우 화살표 아이콘 마커라 **그대로 보존**한다.
예산 14B = `스테이터스`(10B) + `<＞>`(4B) 로 정확히 맞는다.
"""
# raw(cp932 원본 바이트) -> (원문, 번역)
TRANS = {
    bytes.fromhex('83588365815b835e8358' '3c81843e'): ('ステータス<＞>', '스테이터스<＞>'),

    # 장비창 왼쪽 「装備はずす」 — 이것도 앞에 NUL 없는 포인터 표가 붙어 있다
    # (`/RIG2MENU.OVL#1` 0x2841·0x4677 두 곳). 실기에서 초록 글씨로
    # 「친무はずす」처럼 나왔다 — 앞 두 글자는 도너로 뺏긴 칸, 뒤는 미번역 원문.
    bytes.fromhex('919594f582cd82b882b7'): ('装備はずす', '장비 해제'),
}
